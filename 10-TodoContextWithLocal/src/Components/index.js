import TodoForm from "./TodoForm";
import TodoItem from "./TodoItem";
export { TodoForm, TodoItem };
//normally index js files are used to export all the components from a folder, so that we can import them easily in other files. For example, instead of importing TodoForm and TodoItem separately in App.jsx, we can import them together from the Components folder.
